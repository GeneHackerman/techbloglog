//this will force authentication upon login
//if no user, then redirect to homepage
//until user is created
const withAuth = (req, res, next) => {
    if(!req.session.user_id) {
        res.redirect('/login');
    } else {
        next();
    }
};

module.exports = withAuth;